import { InputFileDirective } from './input-file.directive';

describe('InputFileDirective', () => {
  it('should create an instance', () => {
    const directive = new InputFileDirective();
    expect(directive).toBeTruthy();
  });
});
